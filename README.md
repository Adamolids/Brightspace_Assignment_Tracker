# Brightspace_Assignment_Tracker
A Java based synchronization utility for the Brightspace (D2L) LMS. The application fetches course metadata, assignment details, and attachments via the Brightspace REST API, mapping JSON payloads to a local object model using Jackson.
